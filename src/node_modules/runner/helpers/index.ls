export addSeveral = (type, items, receiver) ->
	if typeof! items isnt 'Array' => items = [items]

	receiver ?= []
	for item in items
		if not (item instanceof type)
			item = new type(item)
		receiver.push item

	receiver
