export class Report
	(input) ~>
		spec = switch typeof! input
		| 'Boolean' => {passed}
		| 'Object' => input
		| _ => {}

		# ----------------------------------------------------------------------

		{passed} = spec
		passed ?= false
		@ <<< {passed}

	display: (logger) ->
		logger.log 'ok'
